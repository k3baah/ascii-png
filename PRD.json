{
  "features": [
    {
      "id": "v0-001",
      "category": "scaffold",
      "priority": 1,
      "version": "v0",
      "description": "Project scaffold -- set up the sticker_factory/ package with all module files and update requirements.txt",
      "steps": [
        "Create sticker_factory/ directory with __init__.py, config.py, db.py, models.py, renderer.py, publisher.py, cli.py, ideation.py, ascii_gen.py",
        "Create config.yaml with niche, ideation, ascii_generation, and rendering sections per SPEC.md",
        "Update requirements.txt with all needed dependencies (Pillow, click, pyyaml, python-dotenv, requests)",
        "Verify: `python -c \"import sticker_factory\"` succeeds"
      ],
      "passes": true
    },
    {
      "id": "v0-002",
      "category": "config",
      "priority": 2,
      "version": "v0",
      "description": "Config loading -- load YAML config file and .env secrets into a unified config object",
      "steps": [
        "Implement config.py to load config.yaml using PyYAML",
        "Load .env secrets using python-dotenv (ANTHROPIC_API_KEY, PRINTIFY_API_KEY)",
        "Provide a get_config() function that returns the merged config",
        "Verify: loading config.yaml returns expected niche, rendering, and ideation settings",
        "Verify: missing .env does not crash (secrets are optional until needed)"
      ],
      "passes": true
    },
    {
      "id": "v0-003",
      "category": "database",
      "priority": 3,
      "version": "v0",
      "description": "SQLite schema -- create the designs table with all columns from SPEC.md",
      "steps": [
        "Implement db.py with init_db() that creates the designs table",
        "Columns: id (INTEGER PK), concept_title (TEXT), concept_brief (TEXT), ascii_art (TEXT), ascii_file_path (TEXT), png_file_paths (JSON), colors (JSON), status (TEXT), printify_product_id (TEXT), etsy_listing_url (TEXT), created_at (DATETIME), updated_at (DATETIME)",
        "Provide helper functions: insert_design, get_design, update_design, list_designs",
        "Verify: init_db() creates the table, insert and retrieve a row successfully"
      ],
      "passes": true
    },
    {
      "id": "v0-004",
      "category": "models",
      "priority": 4,
      "version": "v0",
      "description": "Data models -- Python dataclasses for Design matching the SQLite schema",
      "steps": [
        "Implement models.py with a Design dataclass",
        "Fields match the designs table columns",
        "Include from_row() classmethod to construct from a DB row",
        "Include to_dict() method for serialization",
        "Verify: can create a Design instance, round-trip through to_dict() and from_row()"
      ],
      "passes": true
    },
    {
      "id": "v0-005",
      "category": "rendering",
      "priority": 5,
      "version": "v0",
      "description": "Renderer -- refactor ascii2png.py into the renderer module with print-quality sizing",
      "steps": [
        "Move core rendering logic from ascii2png.py into sticker_factory/renderer.py",
        "Support configurable font size (200-400px), padding, and background color (white default)",
        "render_ascii_to_png(ascii_text, output_path, font_size, padding, fg_color, bg_color) function",
        "Output PNGs at print quality (2000-4000px on longest side)",
        "Verify: render a sample ASCII art file to PNG, confirm output dimensions are in print-quality range"
      ],
      "passes": true
    },
    {
      "id": "v0-006",
      "category": "rendering",
      "priority": 6,
      "version": "v0",
      "description": "Color variations -- render the same ASCII design in multiple color schemes from config",
      "steps": [
        "Implement render_variations(ascii_text, output_dir, design_name, color_schemes) in renderer.py",
        "Read color_schemes from config.yaml (each has name, fg, bg)",
        "Output one PNG per color scheme, named like {design_name}_{scheme_name}.png",
        "Verify: given 3 color schemes, produces 3 distinct PNG files with correct colors"
      ],
      "passes": true
    },
    {
      "id": "v0-007",
      "category": "cli",
      "priority": 7,
      "version": "v0",
      "description": "CLI entry point -- `sticker` command with subcommands using click or argparse",
      "steps": [
        "Implement cli.py with a main command group and subcommands: render, publish, review",
        "render subcommand: takes an ASCII file path, renders to PNG (with color variations)",
        "Wire up CLI as a console_scripts entry point or a __main__.py so `python -m sticker_factory` works",
        "Verify: `python -m sticker_factory render designs/vibe-coder.txt` produces PNGs in exports/"
      ],
      "passes": true
    },
    {
      "id": "v0-008",
      "category": "publishing",
      "priority": 8,
      "version": "v0",
      "description": "Printify API client -- basic API wrapper for authentication, creating products, and uploading images",
      "steps": [
        "Implement publisher.py with a PrintifyClient class",
        "Methods: upload_image(image_path) -> image_id, create_product(title, image_id, blueprint_id) -> product_id",
        "Use PRINTIFY_API_KEY from config/env",
        "Handle HTTP errors gracefully with logging",
        "Verify: class instantiates, methods exist with correct signatures (live API test deferred until key is set up)"
      ],
      "passes": true
    },
    {
      "id": "v0-009",
      "category": "publishing",
      "priority": 9,
      "version": "v0",
      "description": "Publish command -- CLI subcommand to take a rendered PNG and publish it to Printify as a sticker product",
      "steps": [
        "Add publish subcommand to CLI: takes a design ID or PNG path",
        "Uploads image to Printify, creates a product, stores product ID in DB",
        "Updates design status to 'published'",
        "Verify: command runs without error when given valid inputs (end-to-end test deferred until API key exists)"
      ],
      "passes": true
    },
    {
      "id": "backlog-001",
      "category": "ideation",
      "priority": 101,
      "version": "backlog",
      "description": "LLM ideation -- Claude API generates concept briefs (title, description, style hints, colors)",
      "steps": [
        "Implement ideation.py with generate_concepts(niche, count) using Claude API",
        "Store generated concepts in SQLite with status 'ideated'",
        "Verify: generates expected number of concept briefs with required fields"
      ],
      "passes": false
    },
    {
      "id": "backlog-002",
      "category": "ascii-generation",
      "priority": 102,
      "version": "backlog",
      "description": "LLM ASCII generation -- Claude API generates ASCII art from concept briefs",
      "steps": [
        "Implement ascii_gen.py with generate_ascii(concept_brief) using Claude API",
        "Save generated ASCII art to designs/ directory and update DB",
        "Verify: given a concept brief, produces valid ASCII art text"
      ],
      "passes": false
    },
    {
      "id": "backlog-003",
      "category": "rendering",
      "priority": 103,
      "version": "backlog",
      "description": "AI image generation (GPT/Gemini) -- v2 design method using image generation APIs",
      "steps": [
        "Add image generation support via ChatGPT image API or Gemini Imagen",
        "Integrate as alternative to ASCII-to-PNG rendering pipeline",
        "Verify: generates a sticker design image from a concept brief"
      ],
      "passes": false
    },
    {
      "id": "backlog-004",
      "category": "rendering",
      "priority": 104,
      "version": "backlog",
      "description": "Sticker sheet composition -- arrange multiple stickers on a single printable sheet",
      "steps": [
        "Implement sheet layout logic to tile multiple sticker PNGs onto one sheet",
        "Configurable sheet size, sticker spacing, and arrangement",
        "Verify: produces a composite PNG with multiple stickers arranged correctly"
      ],
      "passes": false
    },
    {
      "id": "backlog-005",
      "category": "pipeline",
      "priority": 105,
      "version": "backlog",
      "description": "Chain pipeline together -- `sticker run` command that executes ideate -> generate -> render -> review -> publish",
      "steps": [
        "Implement run subcommand in cli.py that chains all pipeline steps",
        "Pause for manual review step unless --auto flag is passed",
        "Verify: full pipeline runs end-to-end from ideation to publishing"
      ],
      "passes": false
    },
    {
      "id": "backlog-006",
      "category": "infra",
      "priority": 106,
      "version": "backlog",
      "description": "Cloud deployment + cron scheduling -- deploy pipeline to run on a schedule",
      "steps": [
        "Containerize the pipeline or deploy to a cloud VM",
        "Set up cron/launchd/Cloud Scheduler to run daily",
        "Verify: pipeline runs unattended on schedule"
      ],
      "passes": false
    },
    {
      "id": "backlog-007",
      "category": "observability",
      "priority": 107,
      "version": "backlog",
      "description": "Observability dashboard (TypeScript) -- web UI showing pipeline stats, design statuses, and error rates",
      "steps": [
        "Build a TypeScript web app that reads from the SQLite database",
        "Display: designs generated, approved/rejected ratio, published count, error rate",
        "Verify: dashboard renders with live data from the pipeline DB"
      ],
      "passes": false
    },
    {
      "id": "backlog-008",
      "category": "notifications",
      "priority": 108,
      "version": "backlog",
      "description": "Daily digest notifications -- Slack/Discord/email summary of pipeline activity",
      "steps": [
        "Implement notification module with Slack/Discord webhook or email support",
        "Send daily summary: new designs, pending reviews, published count, errors",
        "Verify: notification is sent with correct content"
      ],
      "passes": false
    },
    {
      "id": "backlog-009",
      "category": "review",
      "priority": 109,
      "version": "backlog",
      "description": "Automated LLM design review -- Claude-based quality check before publishing",
      "steps": [
        "Implement LLM-based review that evaluates design quality, readability, and aesthetic appeal",
        "Auto-approve, auto-reject, or flag for human review based on confidence",
        "Verify: given a set of test designs, correctly categorizes quality levels"
      ],
      "passes": false
    },
    {
      "id": "dbflow-001",
      "category": "database",
      "priority": 110,
      "version": "dbflow",
      "description": "v2 schema migration -- move to one-row-per-PNG with separate copy/publish state and safe legacy backfill",
      "steps": [
        "Create a v2 designs schema with columns: design_key, concept_id, variation_name, variation_fg, variation_bg, png_path, png_path_canonical, ascii_file_path, listing_title, listing_description, listing_tags (JSON), copy_state, copy_revision, publish_state, published_copy_revision, is_superseded, printify_product_id, etsy_listing_url, last_error, created_at, updated_at",
        "Add active-row uniqueness with partial indexes: UNIQUE(design_key) WHERE is_superseded=0 and UNIQUE(png_path_canonical) WHERE is_superseded=0 AND png_path_canonical IS NOT NULL",
        "Implement safe migration: rename legacy designs table to backup, create v2 table, backfill one row per PNG from legacy data, preserve timestamps, and only then switch reads/writes to v2",
        "Update models.py Design dataclass and db.py helpers (insert_design, get_design, update_design, list_designs) to validate copy_state (missing|ready|error) and publish_state (never|uploaded|published|deleted|error)",
        "Ensure migration is idempotent and keeps old rows as backup until verification is complete",
        "Verify: migration runs cleanly on legacy DB, no row loss, CRUD + uniqueness behavior work on v2 schema"
      ],
      "passes": true
    },
    {
      "id": "dbflow-002",
      "category": "pipeline",
      "priority": 111,
      "version": "dbflow",
      "description": "Concept files -- human-editable JSON files in concepts/ that define creative direction and variation color schemes",
      "steps": [
        "Define concept JSON format: { id, brief, style, keywords, ascii_file, variations: [{scheme, fg, bg}] }",
        "Create concepts/ directory with concept files named {design-name}.json",
        "Concept files are human-editable input; DB references them via concept_id but does not duplicate content",
        "Verify: concept files load correctly and variations array is parseable"
      ],
      "passes": true
    },
    {
      "id": "dbflow-003",
      "category": "rendering",
      "priority": 112,
      "version": "dbflow",
      "description": "Render → DB integration -- render inserts one DB row per variation with concept auto-discovery and idempotency",
      "steps": [
        "Update `sticker render` to insert one DB row per variation (copy_state=missing, publish_state=never, png_path, png_path_canonical, concept_id, variation_name/fg/bg, design_key)",
        "Concept auto-discovery: infer concept_id from ASCII filename (e.g. designs/vibe-coder.txt → concepts/vibe-coder.json). --concept flag overrides",
        "Variation source precedence: concept JSON variations field first, fall back to config.yaml global color_schemes",
        "Idempotent by default: skip variations that already have an active DB row + PNG on disk",
        "--force flag: re-render all variations, mark prior active rows is_superseded=1, insert fresh active rows",
        "Verify: render creates correct DB rows, re-render without --force is a no-op, --force supersedes old rows"
      ],
      "passes": false
    },
    {
      "id": "dbflow-004",
      "category": "publishing",
      "priority": 113,
      "version": "dbflow",
      "description": "DB-first copywrite -- listing copy generation writes directly to DB with revision tracking",
      "steps": [
        "Update `sticker copywrite` to write listing_title, listing_description, listing_tags to DB row, set copy_state=ready, and increment copy_revision",
        "Copywrite must not mutate publish_state (published rows remain published unless explicitly republished)",
        "Support two input modes: file paths/directories (look up active DB row by png_path_canonical) and --no-copy filter (process rows where copy_state=missing)",
        "Retire sidecar writes and add `sticker migrate-sidecars --dry-run|--apply` to import existing .copy.json content into DB without LLM regeneration",
        "Verify: copywrite updates copy fields/revision correctly and sidecar migration imports existing copy deterministically"
      ],
      "passes": false
    },
    {
      "id": "dbflow-005",
      "category": "publishing",
      "priority": 114,
      "version": "dbflow",
      "description": "DB-first publish -- publish reads listing copy from DB, requires copy, supports batch mode",
      "steps": [
        "Update `sticker publish` to read png_path, listing_title, listing_description, listing_tags from DB row (no sidecar fallback)",
        "Refuse to publish unless copy_state=ready and listing_title/listing_description are present; print 'run copywrite first' error",
        "Input modes: `sticker publish --ready` batch-publishes rows where copy_state=ready AND publish_state=never AND is_superseded=0; `sticker publish <id>` publishes a specific row",
        "Add optional `--republish` mode for rows already in publish_state uploaded/published",
        "Update row on publish: set printify_product_id, etsy_listing_url, publish_state (uploaded or published), and published_copy_revision=copy_revision",
        "--etsy/--no-etsy flag controls whether to push to Etsy (default: Printify only)",
        "Verify: publish reads from DB, rejects rows without copy, batch mode works"
      ],
      "passes": false
    },
    {
      "id": "dbflow-006",
      "category": "cli",
      "priority": 115,
      "version": "dbflow",
      "description": "CLI inspection commands -- sticker list and sticker show for browsing design state in the DB",
      "steps": [
        "Add `sticker list` command: table of all images showing id, concept, variation, copy_state, publish_state, is_superseded",
        "Add `sticker list --no-copy` filter: rows where copy_state=missing",
        "Add `sticker list --unpublished` filter: rows where publish_state!=published",
        "Add `sticker list --needs-republish` filter: rows where copy_revision > published_copy_revision",
        "Add `sticker show <id>` command: full detail on one row",
        "Verify: list and show display correct data with filters working"
      ],
      "passes": false
    },
    {
      "id": "dbflow-007",
      "category": "publishing",
      "priority": 116,
      "version": "dbflow",
      "description": "Printify sync -- on-demand command to reconcile DB with current Printify product state",
      "steps": [
        "Add `sticker sync` command that fetches current products from Printify API",
        "Reconcile with DB: for active rows with printify_product_id, set publish_state=deleted when product no longer exists on Printify",
        "Not run automatically; user invokes when needed",
        "Verify: sync detects removed products and updates DB accordingly"
      ],
      "passes": false
    },
    {
      "id": "backlog-013",
      "category": "publishing",
      "priority": 201,
      "version": "backlog",
      "description": "Variant pricing configuration -- set retail prices per variant size in config, apply during publish",
      "steps": [
        "Add pricing map to config.yaml keyed by variant size (2x2, 3x3, 4x4, 6x6)",
        "Update publish command to match Printify variants to config prices by size",
        "Verify: published products have correct retail prices per size variant"
      ],
      "passes": false
    },
    {
      "id": "backlog-014",
      "category": "publishing",
      "priority": 202,
      "version": "backlog",
      "description": "Switch kiss-cut sticker shipping provider from SPOKE to Printify Choice -- use Printify's recommended provider instead of hardcoded SPOKE (provider ID 1)",
      "steps": [
        "Look up the Printify Choice provider ID for kiss-cut stickers (blueprint 1268) via the catalog API",
        "Update config.yaml default print_provider_id from 1 (SPOKE) to the Printify Choice provider ID",
        "Update publish command to use the new provider and fetch its available variants",
        "Verify: published products use the Printify Choice provider with correct variants and pricing"
      ],
      "passes": true
    },
    {
      "id": "backlog-015",
      "category": "publishing",
      "priority": 203,
      "version": "backlog",
      "description": "Change default product mockup -- investigate Printify API for setting which mockup image is used as the primary listing image, and if supported, set it to the 3rd mockup",
      "steps": [
        "Research Printify API docs for mockup/image ordering on product listings (e.g. print_areas, images, or mockup endpoints)",
        "Determine if mockup selection is controllable via the create/update product API",
        "If supported, update publish command to set the 3rd mockup as the primary product image",
        "Add config option for preferred mockup index so it's easily changeable",
        "Verify: newly published products show the 3rd mockup as the primary listing image"
      ],
      "passes": true
    }
  ]
}
